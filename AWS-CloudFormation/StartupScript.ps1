#configure aws cli
aws configure

#Enter the Access Key,
#the Access Secred from your AWS CLI User (created bevor), 
#the default region (eu-central-1),
#the default output format (text, table or json)

#Test Access
aws iam list-users

